import { request } from "../utils/request";


export function login() {
    // request.setCancelKey('login').post('/cjcx/send')
    // request.post('/cjcx/send', {})
    // request.setCancelKey('login').post('/cjcx/send')
    // request.setCancelKey('login').post('/cjcx/send')

    // setTimeout(() => {
    //     request.abort('login')
    // }, 3000);
}